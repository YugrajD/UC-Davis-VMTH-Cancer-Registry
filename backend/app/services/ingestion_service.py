"""
Ingestion service — placeholder for future API-based upload.

The primary ingestion path is the standalone script:
    database/seed/ingest_petbert.py

This module is kept for the /api/v1/ingest/upload endpoint which may be
wired up to a frontend later.
"""
