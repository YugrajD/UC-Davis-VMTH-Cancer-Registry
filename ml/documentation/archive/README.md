# Archive — historical training logs

Phase-by-phase training history from earlier iterations of the project. Preserved for reference. The current production baseline is documented in `ml/documentation/` (parent dir). Do not consult these logs to determine current behavior — they describe approaches that were tried and superseded.
